<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <style name="EditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:textColor">#FFFFFFFF</item>
    </style>

    <style name="spinnerItemStyle" parent="android:Widget.TextView.SpinnerItem">
        <item name="android:padding">2dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/katiewhite</item>
    </style>

    <style name="spinnerDropDownItemStyle" parent="android:TextAppearance.Widget.TextView.SpinnerItem">
        <item name="android:padding">3dp</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/katiewhite</item>
        <item name="android:background">@color/katiered</item>
    </style>

    <style name="settingsPreferenceTheme" parent="AppBaseTheme">
        <item name="android:textColorPrimary">@color/katiewhite</item>
        <item name="android:textColorSecondary">@color/werkwhite </item>
        <item name="android:textColorTertiary">@color/werkwhite</item>
        <item name="android:textSize">17sp</item>

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/SettingActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:windowBackground">@color/katieteal</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <!-- Main Page Theme -->
    <style name="MainTheme" parent="@android:style/Theme.Holo.Light">

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/MainActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowBackground">@color/katieteal</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <!-- Main Page Theme ActionBar -->
    <style name="MainActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/MainActionBarTextColor</item>
    </style>

    <!-- Share My Location Theme -->
    <style name="ShareMyLocationTheme" parent="Theme.AppCompat.Light">

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/ShareMyLocationActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:windowBackground">@color/katienavy</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <!-- ShareMyLocation Theme ActionBar -->
    <style name="ShareMyLocationActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/ShareMyLocationActionBarTextColor</item>
    </style>

    <!-- Emergency Theme -->
    <style name="EmergencyTheme" parent="Theme.AppCompat.Light">

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/EmergencyActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:windowBackground">@color/katiered</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <!-- Emergency Theme ActionBar -->
    <style name="EmergencyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/EmergencyActionBarTextColor</item>
    </style>

    <!-- Setting Theme -->
    <style name="SettingTheme" parent="Theme.AppCompat.Light">

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/SettingActionBar</item>

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:windowBackground">@color/katieteal</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <!-- Setting Theme ActionBar -->
    <style name="SettingActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/SettingActionBarTextColor</item>
    </style>

    <!-- Emergency Title Text Color -->
    <style name="EmergencyActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Main Activity Title Text Color -->
    <style name="MainActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- ShareMyLocation Title Text Color -->
    <style name="ShareMyLocationActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Setting Title Text Color -->
    <style name="SettingActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Gesture Confirmation Page Theme -->
    <style name="GestureConfirmationTheme" parent="@android:style/Theme.Holo.Light">

        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/GestureConfirmationActionBar</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowBackground">@color/katiered</item>
    </style>

    <!-- Gesture Confirmation Theme ActionBar -->
    <style name="GestureConfirmationActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/GestureConfirmationActionBarTextColor</item>
    </style>

    <!-- Gesture Confirmation Title Text Color -->
    <style name="GestureConfirmationActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- ContactList Page Theme -->
    <style name="ContactListTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarTabStyle">@style/ContactListActionBarTab</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <!-- Action bar customization -->
        <item name="android:actionBarStyle">@style/ContactListActionBar</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- ContactList Theme ActionBar -->
    <style name="ContactListActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/katiewhite</item>
        <item name="android:titleTextStyle">@style/ContactListActionBarTextColor</item>
    </style>

    <!-- ContactList Title Text Color -->
    <style name="ContactListActionBarTextColor" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- ContactList Tab Style -->
    <style name="ContactListActionBarTab" parent="@android:style/Widget.Holo.ActionBar.TabView">
        <item name="android:background">@drawable/tab_bar_background</item>
    </style>

    <!-- ContactList Tab Text Style -->
    <style name="TabTextStyle" parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@drawable/tab_text</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:editTextStyle">@style/EditTextStyle</item>
        <item name="android:windowBackground">@color/werkwhite</item>

        <!-- For the resting Spinner style -->
        <item name="android:spinnerItemStyle">@style/spinnerItemStyle</item>

        <!-- For each individual Spinner list item once clicked on -->
        <item name="android:spinnerDropDownItemStyle">@style/spinnerDropDownItemStyle</item>
    </style>

    <style name="ProgressDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:alertDialogStyle">@style/CustomAlertDialogStyle</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:windowBackground">@color/katiewhite</item>
        <item name="android:textColor">@color/katienavy</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:textColorSecondary">@color/katienavy</item>
        
    </style>

    <style name="CustomAlertDialogStyle">
        <item name="android:bottomBright">@color/katiewhite</item>
        <item name="android:bottomDark">@color/katiewhite</item>
        <item name="android:bottomMedium">@color/katiewhite</item>
        <item name="android:centerBright">@color/katiewhite</item>
        <item name="android:centerDark">@color/katiewhite</item>
        <item name="android:centerMedium">@color/katiewhite</item>
        <item name="android:fullBright">@color/katiewhite</item>
        <item name="android:fullDark">@color/katiewhite</item>
        <item name="android:topBright">@color/katiewhite</item>
        <item name="android:topDark">@color/katiewhite</item>
        <item name="android:windowBackground">@color/katiewhite</item>
        <item name="android:textColor">@color/katiered</item>
        <item name="android:windowFrame">@null</item>
    </style>

</resources>