<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="sound"
        android:summary="Turn the sound on or off"
        android:title="Sound" />

    <!--
    NOTE: ListPreference's summary should be set to its value by the activity code. 
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/edit_emergency_contacts" />
    

    <MultiSelectListPreference
        android:defaultValue="@array/empty_array"
        android:dialogTitle="@string/edit_emergency_contacts"
        android:entries="@array/emergency_contacts"
        android:entryValues="@array/emergency_contacts_values"
        android:key="mode_repeat"
        android:summary=""
        android:title="@string/edit_emergency_contacts" />
	-->
</PreferenceScreen>